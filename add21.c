#include <stdio.h>
int main() {    

    int number1, number2, sum;
    number1=21;
    number2=20;

    // calculate the sum
    sum = number1 + number2;      
    
    printf("%d + %d = %d", number1, number2, sum);
    return 0;
}
